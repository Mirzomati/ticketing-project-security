version: '3.7'
services:
  mypostgres:
    image: postgres
    container_name: mypostgres
    environment:
      POSTGRES_USER: "postgres"
      POSTGRES_PASSWORD: "admin"
      POSTGRES_DB: "ticketing-app"
  ticketing-app:
networks:
  webnet:
    driver: bridge
volumes:

